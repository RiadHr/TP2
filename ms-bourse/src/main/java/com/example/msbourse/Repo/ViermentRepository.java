package com.example.msbourse.Repo;

import com.example.msbourse.entities.Vierment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ViermentRepository extends JpaRepository<Vierment, Long> {
}